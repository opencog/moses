#INCLUDE_DIRECTORIES(
#	${PYTHON_INCLUDE_PATH}
#)

IF (HAVE_NOSETESTS)
	ADD_TEST(CythonMoses ${NOSETESTS_EXECUTABLE} -vs
		${CMAKE_SOURCE_DIR}/tests/cython/moses/)
	SET_TESTS_PROPERTIES(CythonMoses
	PROPERTIES ENVIRONMENT "PYTHONPATH=${PROJECT_BINARY_DIR}/moses/cython")
ENDIF (HAVE_NOSETESTS)
